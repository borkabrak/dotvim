" Quickfix window
" set cursorline

"  Use the default behavior for the Enter key instead of my oh-so-clever
"  mapping (unconditionally open filename under cursor in current window)

nnoremap <buffer> <Enter> <Enter>
